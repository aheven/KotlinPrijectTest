<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="heven.greendaotest">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".SampleApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.NetworkActivity"
            android:label="@string/name_activity_network" />
        <activity
            android:name=".ui.NettyActivity"
            android:label="@string/name_activity_netty" />

        <activity
            android:name=".ui.SurfaceFrameAnimationActivity"
            android:label="@string/name_activity_frame_surface" />

        <activity android:name=".ui.RiseNumberActivity"
            android:label="@string/name_activity_rise_number" />

        <activity android:name=".ui.BottomBarActivity"
            android:label="@string/name_activity_bottom_bar" />

        <activity
            android:name="com.tencent.bugly.beta.ui.BetaActivity"
            android:theme="@android:style/Theme.Translucent" />

    </application>

</manifest>